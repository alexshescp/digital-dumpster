
import { EmailDumpForm } from "@/components/EmailDumpForm";

const Index = () => {
  return (
    <div className="min-h-screen bg-gradient-to-br from-primary/10 to-secondary/30 py-12">
      <EmailDumpForm />
    </div>
  );
};

export default Index;
